<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DSA Code Runner</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/index.css">
    
</head>
<body>
      <%- include("./includes/navbar.ejs")%>

        <div class=" main" style="display: flex; ">
            <div >
                <div class="mt-3">
                <h3 class="ms-4 title"><b><%= question.title %></b></h3>
                </div>
                <br>
                <div class="left ms-4">
                    <p class="description"><%= question.description %></p>
                </div>

                <div class="testcase-card ms-3">
                    <div class="first card mb-4">
                        <p><strong>Input:</strong></p>
                        <pre><%= question.testcases[0].input %></pre>

                        <p><strong>Expected Output:</strong></p>
                        <pre><%= question.testcases[0].expected_output %></pre>
                    </div>

                    <div class="second card mb-4">
                        <p><strong>Input:</strong></p>
                        <pre><%= question.testcases[1].input %></pre>

                        <p><strong>Expected Output:</strong></p>
                        <pre><%= question.testcases[1].expected_output %></pre>
                    </div>

                    <div class="third card mb-4">
                        <p><strong>Input:</strong></p>
                        <pre><%= question.testcases[2].input %></pre>

                        <p><strong>Expected Output:</strong></p>
                        <pre><%= question.testcases[2].expected_output %></pre>
                    </div>
                    
                </div>

            </div>
        
            <form method="POST" action="/topic/<%= question._id %>">
                <div class="right ms-5 mt-5">
                    <label for="language">Language:</label>
                    <select id="language" name="language" class="form-select w-50">
                        <option value="54">C++</option>
                        <option value="62">Java</option>
                        <option value="71">Python</option>
                    </select>
                    <br>
                    <textarea id="code" name="code" rows="25" cols="62" class="form-control">// Write your code here</textarea>
                    <br>
                    <button type="submit" name="action" class="btn btn-primary" value="run">Run Code</button>

                    <br><br>
                </div>
            </form>

           

        </div>
</body>
</html>
